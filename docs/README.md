# suisoku docs

Vercel 向けの最小構成を配置しています。
見た目は共通の通常CSSで整えており、追加の tailwindcss は使っていません。版番号は `lib/app-info.ts` の共通値を使い、入口画面の見出しとブラウザのタブ名が必ず同じ表示になります。

入口画面では、ネオアイムジャグラーEX と マイジャグラーV と ゴーゴージャグラー3 と Sファンキージャグラー2 と Sミスタージャグラー と ジャグラーガールズSS と ウルトラミラクルジャグラー と ハッピージャグラーVⅢ を左列、カバネリ2 と スマスロ Lハナビ と 新ハナビ と ハナビBH と スマスロ サンダーV と バーサス リヴァイズ と ハナハナホウオウ ～天翔～ と ニューキングハナハナV を右列に分けた2列表示にしています。各列の中ではボタンを縦に並べ、狭い画面でも左右の列を保ったまま押せるようにしています。各機種ページの上部には、入口画面へ戻る「トップページに戻る」リンクを表示します。

各画面では、入力欄の名前を左、入力欄を右に並べています。最後に期待値用の条件と店情報の入力欄があります。ハナビBH、スマスロ Lハナビ、新ハナビでは攻略率を独立した入力欄で使い、初期値は75%です。ハッピージャグラーVⅢ と Sファンキージャグラー2 と Sミスタージャグラー と ジャグラーガールズSS と ウルトラミラクルジャグラー と マイジャグラーV と ネオアイムジャグラーEX では攻略率は使わず、公表値、チェリー狙い、フル攻略の3択を横一列のラジオボタンで選びます。狭い幅でも1列のまま見られるように、必要な場合だけ横へ送って確認できる形にしています。これら7機種の初期状態はチェリー狙いです。カバネリ2、ハナハナホウオウ ～天翔～、ゴーゴージャグラー3 では攻略率は使わず、公表の機械割や出玉率をそのまま期待値に使います。見出しの右隣に「期待値の計算に使用」と表示するページでは、その内容を機械割計算へ反映します。その下の店情報では貸枚数は46、交換率は5.0が初期値です。店情報では貸枚数と交換率の入力欄を短め、現金投資は少し広めにしてあります。貸枚数の右に1枚あたりの貸し単価、交換率の右に1枚あたりの交換単価、現金投資の右に換金ギャップ損をリアルタイム表示します。

各ページの入力内容は、その端末のブラウザ内へ自動で保存します。同じ端末、同じブラウザで開き直したりリロードしたりしても、前回の入力内容を戻せます。さらに、保存データとして保存1から保存5までの5枠を用意し、保存データを押すとその枠の内容を読み込みます。保存データ名は各枠の下の入力欄で変更でき、空欄のときは従来どおり保存1から保存5までの名前を表示します。画面を開いた時点で、保存済みの枠があれば一番小さい番号の保存データを自動で選び、保存済みの枠がない場合は保存1を選びます。保存データを1つだけ選んでいるときは、内容を入力した時点でその枠へ自動保存します。保存データは複数選択でき、複数選んだときは数値入力を合算した内容を入力欄へ表示し、そのまま推測できます。複数選択中は合算表示として扱い、個別の保存データへ合算値を自動上書きしません。選択中の保存データが1つだけのときに同じ保存データをもう一度押しても、未選択には戻しません。複数選択を外す場合も、最後の1つは選択したまま残します。未保存の枠を押したときは初期状態の入力内容を表示します。貸枚数、交換率、攻略率、打ち方の選択は合算せず、保存されている値を引き継ぎます。推測結果そのものは保存せず、入力だけを残します。クリアボタンは保存データを1つだけ選んでいる場合はその保存データと名前を初期状態へ戻し、複数選択中は合算表示を解除して保存データを1つ選んだ状態に戻します。全てクリアボタンは保存1から保存5までの全保存データ、名前、表示中の入力内容を初期状態へ戻し、その後は保存1を選んだ状態にします。スマスロ Lハナビ と 新ハナビ と スマスロ サンダーV と バーサス リヴァイズ では、入力モードの切替状態も端末内へ保存します。

名前にジャグラーを含むページでは、小役入力の近くに3行構成のカウンターを表示します。上部には回転用の丸いボタンも置いてあり、押すたびにカウンター全体を左へ90度回した横向き表示と、元の縦向き表示を切り替えられます。1行目には横幅を広げた押下確認用メーターと、現在のブドウ数を4桁で示すデジタル表示風の数値を並べています。メーターは5段で構成し、ボタンを押すたびに帯が1本ずつ追加され、最大5本まで残しながら、それぞれが60秒かけて右端から左へ向かってなめらかに空になります。ブドウ+1の帯の色は押すたびに順番で切り替わり、同じ色が続かないようにしています。ブドウ-1と単独REG-1は白系の帯と演出にし、単独REG+1は灰色系の帯と演出にしています。ブドウ数のデジタル表示は、ブドウ+1で使われた帯と同じ系統の文字色へ切り替わるため、最後に押された加算色を数字側でも確認できます。ブドウ-1と単独REGの操作では、ブドウ数の文字色は変えません。さらに、ボタンを押した瞬間にはカウンター全体へ同系色のフラッシュと短い揺れを入れ、押せたことを周辺視野でも把握しやすくしています。2行目には左にブドウ-1、右に横幅を広げたブドウ+1ボタン、単独REGの設定差がある Sファンキージャグラー2、マイジャグラーV、ネオアイムジャグラーEX では3行目に左に単独REG-1、右に横幅を広げた単独REG+1ボタンを表示します。4つのボタンの左右端は上段のメーターとカウンターの外端にそろえ、ブドウの2つのボタンは従来のおよそ2倍の縦幅にしています。回転表示では、小役カウンター全体を縦横比を保ったまま拡大し、回転後の左右の余白がほぼ埋まるところまで横幅いっぱいに広げます。ボタンを押すたびに通常時のブドウ数だけを1つ増減し、自動保存の対象にもなります。ブドウ-1では0未満にならないようにしています。スマホで小役カウンターとして押しやすいように、ブドウ用ボタンは推測ボタンよりさらに大きい緑系のボタンにしています。単独REG+1はBAR図柄をイメージした灰色系のボタン、単独REG-1は「-1」だけの小さめ表示にしています。ハナハナホウオウ ～天翔～ と ニューキングハナハナV では、この見た目をそのまま流用したベル用カウンターを通常時小役の入力欄の下へ表示し、ベル+1とベル-1を押せます。ベル用は黄系の配色へ切り替え、図形もベルを模した形にしています。

スマスロ Lハナビ、新ハナビ、スマスロ サンダーV、バーサス リヴァイズのユニメモ画像読み取りは、公式アプリが生成した縦長画像1枚と、ユーザーが画面を分けて撮った複数画像の両方を同じ画像選択から受け付けます。複数画像は選択順にGeminiへ渡し、機種ごとの表示順、黄色い見出し、前後の項目で区分を判別します。同じ部分が重なって写っている場合は同じ項目として扱い、回数を足しません。見出しも前後項目もなく区分が判別できない項目は入力しません。画像は最大8枚まで、1枚10MB以内、合計20MB以内です。

推測ボタンを押すと、入力値から実践G数や各確率を計算して結果を表示します。ボーナス確率の推測では、開始前の値を差し引かず、現在のG数、BIG、REGの累計値をそのまま使います。小役やBIG中、REG中などの推測では、ユーザーの実践分だけを分母に使います。BIG中の項目はBIG中の消化G数を分母に使い、REG中の項目はREG中の消化G数を分母に使います。

推測結果では、総合推測の割合を最初に表示し、その下の実践期待値の表で実践G数、機械割、各設定の期待値、推測割合、推測期待値、およびその合計期待値を表示します。合計行の機械割には、各設定の機械割を推測割合で重み付けした期待機械割も表示します。さらに、合計の推測期待値をもとに700Gあたりで換算した期待時給も表示します。期待時給は「期待時給 1399.6円 / h」のように、名前の右隣へ横並びで表示します。攻略率を使うページでは、機械割の見出し下に攻略率を表示し、通常時の機械割と完全攻略の機械割の間で値を変えます。攻略率を使わないページでは、公表値や選択した打ち方の機械割をそのまま使います。円換算は交換率から自動で変わり、現金投資額ぶんの換金ギャップ損も差し引いています。

カバネリ2 では、開始前と現在の総G数、実践中に数えた下段ベル回数、3周期目と4周期目の試行回数・当選回数を入力して設定1から設定6までを推測します。総G数の差分を下段ベルの分母に使い、周期当選は試行を分母、当選を分子として扱います。3周期目または4周期目が未入力ならその項目だけを総合推測から外し、入力済みの項目は下段ベルと掛け合わせて総合推測へ反映します。当選が試行を超える入力は推測に使わず、入力し直すための案内を表示します。生駒pt欄では「発光なし」と「発光不明」で1pt、「発光あり」と「高確率」で15ptを加算し、0から始まる現在の合計ptを常時表示します。無名pt欄も同じ加算値と合計表示を使います。無名の4ボタンは赤系、生駒の4ボタンは緑系で表示し、それぞれの左に対応する1回分を取り消すマイナスボタンを置きます。加算ボタン内は人物名を省略し、「発光なし」「発光あり」「発光不明」「高確率」の順に表示します。マイナス操作は該当種類の押下回数とポイントを戻し、「発光なし」「発光あり」では発光率用回数も同時に戻します。該当種類の現在回数が0なら減算せず、マイナスボタンの色は回数にかかわらず常に通常の濃さで表示します。無名は108pt、生駒は106ptを100%とする現在区間の規定pt到達率を現在ptから常時計算し、表示は120%を上限とします。合計の規定pt到達率は、CZ当選済みの各履歴を個別に最大120%へ制限してから平均し、現在貯めている途中のptは含めません。「無名CZ当選」または「生駒CZ当選」を押すと、該当人物の現在ptと現在区間の発光回数を履歴へ移して現在値を0へ戻します。当選履歴は新しい順に並べ、回数、当選CZ、その区間の獲得ptと到達率、発光回数と発光率を表示します。履歴の5列目にある削除ボタンで1件ずつ削除でき、削除した履歴ぶんを合計規定pt到達率と発光率から除外します。各人物の常時表示する発光率は履歴と現在区間の回数を合算して求めるため、CZ当選後も累計値を維持します。「発光不明」と「高確率」の操作は発光率に含めません。両方のpt、各ボタンの現在回数、発光率用回数、CZ当選履歴、発光率推測スイッチの状態を保存データの保存・合算・クリア対象に含めます。現在区間の規定pt到達率は設定推測に使いません。発光率は履歴と現在区間の回数を合算し、発光率推測スイッチがONのときだけ無名・生駒を個別に総合推測へ加えます。スイッチの初期状態はOFFです。無名・生駒それぞれの履歴平均による合計規定pt到達率は総合推測に加え、履歴0件の人物は推測から外します。履歴平均が100%を超える場合は推測計算上のみ100%として扱います。マイスロのサミートロフィーは銅・銀・金・キリン・虹を個別集計し、1回以上出現した最も強い色に従って候補設定を除外します。銅は設定2以上、銀は設定3以上、金は設定4以上、キリンは設定5以上、虹は設定6を確定とします。アイテムくじは全出現回数を分母、ツラヌキ筒を分子として、設定4は20%、設定1・2・3・5・6は30%の出現率で総合推測へ加えます。また、来栖の刀は設定2否定、無名のけん玉は設定1否定、小吉は設定2以上、中吉は設定4以上、大吉は設定6確定として候補設定を除外します。マイスロのボイス男女比は女性と男性だけを母数とし、景行、無名特殊、ボイス無しは母数から除外します。ただしNo.3の無名特殊が1回以上なら設定2以上、ボイス無しが1回以上なら設定5以上の確定要素として候補設定を除外します。No.17〜19の景行ボイス合計は全ボイス回数を母数とし、設定1〜3は12%、設定4〜6は19%の出現率で総合推測へ加えます。キャラ紹介も女性と男性だけを母数として男女比を総合推測へ加え、美馬は男女比の母数から除外します。ただし美馬が1回以上なら設定4以上の確定要素として候補設定を除外します。各項目ごとの推測値にも「無名 合計pt到達率」「生駒 合計pt到達率」「サミートロフィー」「アイテムくじ ツラヌキ筒」「アイテムくじ 確定示唆」「無名 発光率」「生駒 発光率」「サンド目停止ボイス 確定示唆」「景行ボイス合計」「サンド目停止ボイス」「キャラ紹介」「キャラ紹介 確定示唆」の実測値と設定割合を表示します。設定別スペックは「機械割」「pt到達率」「発光率」「周期別当選率」「下段ベル」「サンド目停止ボイス」「キャラ紹介」の7表に分けて表示します。機械割表には、表示専用で推測計算に使わないボーナス初当たりとST確率も併記します。ボーナス初当たりは設定1から順に 1/254.2、1/242.3、1/239.6、1/214.0、1/203.2、1/195.1、ST確率は 1/422.5、1/405.9、1/398.7、1/357.2、1/332.6、1/318.5 です。pt到達率は両CZとも設定1から順に 84.7%、84.7%、84.7%、71.8%、62.6%、62.6% です。発光率は無名・生駒とも設定1から順に 10.0%、12.0%、14.0%、16.0%、18.0%、20.0% です。サンド目停止ボイスの女性・男性比率は奇数設定45%・55%、偶数設定55%・45%、キャラ紹介の女性・男性比率は奇数設定40%・60%、偶数設定60%・40%です。下段ベル確率は設定1から順に 1/121.1、1/114.4、1/112.8、1/106.2、1/104.2、1/99.1、3周期目の当選率は 18.4%、23.8%、21.1%、28.5%、32.4%、37.1%、4周期目の当選率は 33.6%、35.2%、36.3%、40.2%、43.4%、46.9%、機械割は 97.5%、98.5%、100.8%、106.0%、111.0%、114.9% です。推測結果には各項目だけで見た設定割合も表示します。店情報と保存1から保存5までの保存データは他機種と同じ仕組みを使います。

入力欄の先頭は無名ptを左、生駒ptを右の2列で表示し、その後に発光率推測スイッチ、開始G数・現在G数・下段ベル、周期当選の順で表示します。開始G数・現在G数・下段ベルは専用の見出しを付けず、各ラベルを入力欄の左へ置いて横一列にまとめます。下段ベル入力欄には1回ずつ増減するボタンを表示し、0未満にはしません。無名pt・生駒ptの各取り消しボタンは「−」だけを表示します。周期当選の3周期目・4周期目では、試行と当選の各入力欄に1回ずつ増減するボタンを表示し、0未満にはしません。G数と下段ベルは任意入力とし、両方がそろっている場合だけ下段ベルを推測へ加えます。G数がない場合もCZ当選履歴、マイスロ、周期当選から推測でき、実践期待値は「−」で表示します。期待時給は推測割合による期待機械割を1時間700Gと現在の交換率で換算して表示し、G数がない場合は現金投資による換金ギャップ損を含めません。CZ当選履歴の表は、ほかのスペック表より小さい文字でコンパクトに表示し、獲得ptと到達率、発光回数と発光率をそれぞれ1行にまとめます。

無名pt・生駒ptは横2列に並べ、各列の中を「現在のpt」「平均到達率」「発光率」「発光なし」「発光あり」「発光不明」「高確率」「CZ当選」の順で縦一列にまとめます。各取り消しボタンは独立した段にせず、対応する加算ボタンと同じ行の左側に表示します。

カバネリ2 のマイスロ入力欄には、ほかのアプリからコピーした表示内容をそのまま貼り付けられます。画面上の集計欄は「サンド目停止時ボイス」「キャラ紹介」「アイテムくじ」「サミートロフィー」の順に表示し、長い説明文と正常読取時の件数報告は表示しません。サミートロフィーは銅・銀・金・キリン・虹の獲得数を個別表示します。「キャラ紹介」の範囲を抽出し、No.1〜3を女性キャラ、No.4〜6を男性キャラ、No.7を美馬として、各回数とキャラ紹介全体に占める割合を表示します。アイテムくじは全項目の回数を合算し、No.1のツラヌキ筒について回数と全体に占める割合を表示します。No.4の来栖の刀、No.5の無名のけん玉、No.8の小吉、No.9の中吉、No.10の大吉も回数を個別表示し、それぞれの否定・確定範囲を推測へ適用します。「サンド目停止時ボイス」の範囲も別に抽出し、No.1・No.2・No.4〜8を女性ボイス、No.9〜16を男性ボイス、No.3を「無名特殊(1否定)」、No.17を「景行(弱)」、No.18を「景行(中)」、No.19を「景行(強)」、No.20を「ボイス無し(56確)」として個別に集計し、各回数とボイス全体に占める割合を表示します。表示順は女性ボイス、男性ボイス、景行の弱・中・強、無名特殊、ボイス無し(56確)とします。キャラ紹介とボイスの各集計欄では割合を大きく先に表示し、回数はその右隣へ小さく添えます。貼り付け内容は保存データに含め、複数の保存データを合算した場合は各内容をまとめて集計します。推測ではトロフィーの確定範囲、ツラヌキ筒の出現率、No.3とボイス無しの確定範囲、景行ボイス合計の出現率を適用します。ボイスとキャラ紹介の男女比では女性と男性だけを使い、特殊枠は母数から除外します。美馬が1回以上なら設定4以上の確定範囲を推測へ適用します。

ハナビBH では、ボーナス、通常時小役、花火チャレンジ中、花火ゲーム中、BIG中について、各項目ごとの推測値を表示します。合算と2役合算は表示だけで、総合推測には含めていません。

ハッピージャグラーVⅢ では、ボーナスと通常時小役のブドウについて、各項目ごとの推測値を表示します。ボーナス合算は表示だけで、総合推測には含めていません。期待値計算は攻略率ではなく、公表値、チェリー狙い、フル攻略の選択によって機械割を切り替え、初期状態はチェリー狙いです。ブドウが未入力のときは、ブドウを0個として扱わず、総合推測から外してボーナス確率のみで推測します。

Sミスタージャグラー では、ボーナスと通常時小役のブドウについて、各項目ごとの推測値を表示します。ボーナス合算は表示だけで、総合推測には含めていません。期待値計算はハッピージャグラーVⅢ と同じく、公表値、チェリー狙い、フル攻略の選択によって機械割を切り替え、初期状態はチェリー狙いです。ブドウが未入力のときは、ブドウを0個として扱わず、総合推測から外してボーナス確率のみで推測します。ブドウ逆算では差枚数を入力し、開始前G数からの実践分、実践G数の直接入力、総G数のどれを使うか選べます。推定ブドウ数と推定確率は表示だけを先に行い、「ブドウ欄へ反映」を押したときだけ通常時小役のブドウ入力へ反映します。

ジャグラーガールズSS では、ボーナスと通常時小役のブドウについて、各項目ごとの推測値を表示します。ボーナス合算は表示だけで、総合推測には含めていません。期待値計算はハッピージャグラーVⅢ と同じく、公表値、チェリー狙い、フル攻略の選択によって機械割を切り替え、初期状態はチェリー狙いです。ブドウが未入力のときは、ブドウを0個として扱わず、総合推測から外してボーナス確率のみで推測します。ブドウ逆算では差枚数を入力し、開始前G数からの実践分、実践G数の直接入力、総G数のどれを使うか選べます。推定ブドウ数と推定確率は表示だけを先に行い、「ブドウ欄へ反映」を押したときだけ通常時小役のブドウ入力へ反映します。

ウルトラミラクルジャグラー では、ボーナスと通常時小役のブドウについて、各項目ごとの推測値を表示します。ボーナス合算は表示だけで、総合推測には含めていません。期待値計算はハッピージャグラーVⅢ と同じく、公表値、チェリー狙い、フル攻略の選択によって機械割を切り替え、初期状態はチェリー狙いです。機械割の公表値には市場調査値を使います。ブドウが未入力のときは、ブドウを0個として扱わず、総合推測から外してボーナス確率のみで推測します。

スマスロ Lハナビ では、ボーナス、通常時小役、花火チャレ中、花火ゲーム中、BIG中、REG中、REGピース画面について、各項目ごとの推測値を表示します。通常時小役では風鈴Aと風鈴Bをそれぞれ総合推測に使い、風鈴合算は表示だけにしています。ボーナス合算も表示だけで、総合推測には含めていません。スペック表の基本スペックにもボーナス合算を表示します。入力モードは通常とユニメモの2種類で、初期状態はユニメモです。ユニメモ画像を選ぶと、サーバー側でGeminiを使って総プレイ数、総BB回数、総RB回数、通常時小役、RT中小役、BB中小役、RB中小役、RB終了画面ピース表示回数を読み取り、読み取れた項目だけを入力へ反映します。Geminiの接続には `.env.local` の `GEMINI_API_KEY` を使い、モデルは `GEMINI_MODEL` が未指定の場合 `gemini-2.5-flash-lite` を使います。画像解析は機種追加を前提に、Geminiへ送る共通処理と機種ごとの読み取り項目を分けています。花火チャレ、花火ゲーム、BIG中、REG中だけは切替に応じて入力形式が変わり、ユニメモでは回数と確率を直接入力し、内部では回数と確率から必要な消化G数を組み立てて推測します。REGピース画面は切替に関係なく、2回以下、3～4回、5回以上ごとに試行回数と発生回数を入力でき、設定1は出現なし、設定2、設定5、設定6は同じ出現率として推測に使います。ユニメモ画像から読み取ったRB終了画面ピース表示回数は、2回以下の試行回数と発生回数へ同じ数を入れます。スペック表にもREGピース画面の出現率を表示します。

新ハナビ では、ボーナス、通常時小役、花火チャレ中、花火ゲーム中、BIG中、REG中、REGピース画面について、各項目ごとの推測値を表示します。通常時小役では風鈴Aと風鈴Bをそれぞれ総合推測に使い、風鈴合算は表示だけにしています。通常時小役の入力欄には氷A、チェリーA2、チェリーBもあります。ボーナス合算も表示だけで、総合推測には含めていません。スペック表の基本スペックにもボーナス合算を表示します。入力モードは通常とユニメモの2種類で、初期状態はユニメモです。ユニメモ画像から、基本情報、通常時小役、RT詳細、BB中詳細、RB中詳細を読み取り、読み取れた項目だけを入力へ反映します。花火チャレ、花火ゲーム、BIG中、REG中だけは切替に応じて入力形式が変わり、ユニメモでは回数と確率を直接入力し、内部では回数と確率から必要な消化G数を組み立てて推測します。REGピース画面は切替に関係なく従来どおりで、0~4回時、5~6回時、7回以上時ごとに試行回数と発生回数を入力でき、入力欄は3桁程度の短めの幅にしています。画像どおりの発生率を使って総合推測と各項目別推測へ反映します。

スマスロ サンダーV では、ボーナス、通常時小役、ボーナス中について、各項目ごとの推測値を表示します。通常時小役ではベルAとベルBをそれぞれ総合推測に使い、ベル合算は表示だけにしています。チェリーB、スイカA、スイカBも個別項目として使い、スイカ合算も表示します。ボーナス合算も表示だけで、総合推測には含めていません。入力モードは通常とユニメモの2種類で、初期状態はユニメモです。ユニメモ画像から、基本情報、小役、BB中小役またはボーナス中小役を読み取り、読み取れた項目だけを入力へ反映します。切替で変わるのはボーナス中だけで、ユニメモではベルB回数と確率から必要な消化G数を組み立て、ベルC回数とリーチ目回数も同じ消化G数に乗せて推測します。

バーサス リヴァイズ では、ボーナス、通常時小役、VS CHANCE中、VS GAME中、BIG中、REG中、RB終了時の赤帯について、各項目ごとの推測値を表示します。通常時小役ではベルAとベルBをそれぞれ総合推測に使い、ベル合算は表示だけにしています。スイカAとチェリーBも総合推測に使います。ボーナス合算も表示だけで、総合推測には含めていません。入力モードは通常とユニメモの2種類で、初期状態はユニメモです。ユニメモ画像から、基本情報、小役、RT詳細、BB中詳細、RB中詳細を読み取り、読み取れた項目だけを入力へ反映します。VS CHANCE中、VS GAME中、BIG中、REG中は切替に応じて入力形式が変わり、ユニメモでは回数と確率から必要な消化G数を組み立てて推測します。RB終了時の赤帯は新ハナビの特殊画面と同じ考え方で、通常時、RT2連まで、RT3連以上に分けて、各条件ごとの試行回数と発生回数を入力できます。

ハナハナホウオウ ～天翔～ では、ボーナス、通常時ベル、BIG中小役、BIG後トップランプ、REG中サイドランプ、連チャンサウンド選択率について、各項目ごとの推測値を表示します。攻略率やユニメモ切替はありません。通常時ベルの入力欄の下にはベル用カウンターも表示します。BIG中小役は通常時ベルの下に表示し、スイカ回数とハズレ回数だけを入力し、分母は実践中のBIG回数×24Gを自動表示して推測に使います。BIG後トップランプは青から虹までの5種類の回数を入力し、分母は実践中のBIG回数を使います。計は表示だけで、総合推測には含めていません。REG中サイドランプは青から虹までの5種類の回数を入力し、分母は実践中のREG回数を使います。ランプ系とBIG中小役の入力欄は短めの幅にし、横方向へ詰めて表示します。連チャンサウンド選択率は新ハナビの特殊画面と同じ考え方で、試行回数と発生回数を入力します。

ニューキングハナハナV では、ボーナス、通常時ベル、BIG前半パート、BT中ビタ押し時サイドランプ、BIG後ハイビスカスランプ、REG後ハイビスカスランプ、レトロサウンド発生率、REG中スイカについて、各項目ごとの推測値を表示します。攻略率やユニメモ切替はありません。通常時ベルの入力欄の下にはベル用カウンターも表示します。BIG前半パートは消化G数を分母にしてスイカ回数とハズレ回数を入力します。BT中ビタ押し時サイドランプとBIG後ハイビスカスランプは、試行回数と5色の発生回数を入力します。REG後ハイビスカスランプは白、青、黄、緑、紫の回数を入力し、最も強い示唆を結果欄へ表示します。青以上が1回でもあれば、その確定条件に合わせて総合推測でも下位設定を0%扱いにします。レトロサウンド発生率は試行回数と発生回数を入力し、REG中スイカはREG中の消化G数を分母にして推測します。スペック表の最後には、REG後ハイビスカスランプの色ごとの設定示唆表も別に表示します。

ゴーゴージャグラー3 では、ボーナスと通常時小役のぶどうについて、各項目ごとの推測値を表示します。ユニメモ切替や攻略率はありません。期待値計算にはチェリー狙い時の機械割をそのまま使います。チェリー狙い時の機械割は設定1から順に 98.28%、99.34%、100.65%、103.05%、105.46%、108.46% です。ボーナス合算は表示だけで、総合推測には含めていません。ぶどうが未入力のときは、ぶどうを0個として扱わず、総合推測から外してボーナス確率のみで推測します。ブドウ逆算では差枚数を入力し、開始前G数からの実践分、実践G数の直接入力、総G数のどれを使うか選べます。推定ブドウ数と推定確率は表示だけを先に行い、「ブドウ欄へ反映」を押したときだけ通常時小役のぶどう入力へ反映します。

Sファンキージャグラー2 では、ボーナス、通常時小役のブドウ、単独REGについて、各項目ごとの推測値を表示します。ユニメモ切替や攻略率はありません。期待値計算はハッピージャグラーVⅢ と同じく、公表値、チェリー狙い、フル攻略の3択で機械割を切り替え、初期状態はチェリー狙いです。ボーナス合算は表示だけで、総合推測には含めていません。ブドウと単独REGはどちらも未入力なら総合推測から外し、入力された項目だけを使って推測します。単独REGは回数のみを入力し、分母には実践ゲーム数を使います。ブドウ逆算では差枚数を入力し、開始前G数からの実践分、実践G数の直接入力、総G数のどれを使うか選べます。推定ブドウ数と推定確率は表示だけを先に行い、「ブドウ欄へ反映」を押したときだけ通常時小役のブドウ入力へ反映します。ブドウ確率は設定1から順に 1/5.96、1/5.92、1/5.88、1/5.87、1/5.79、1/5.72、チェリー狙い時の機械割は 98.00%、99.40%、101.20%、103.50%、106.10%、111.30% です。

マイジャグラーV では、ボーナス、通常時小役のブドウ、単独REGについて、各項目ごとの推測値を表示します。ユニメモ切替や攻略率はありません。期待値計算はハッピージャグラーVⅢ と同じ考え方で、公表値、チェリー狙い、フル攻略の3択で機械割を切り替え、初期状態はチェリー狙いです。ボーナス合算は表示だけで、総合推測には含めていません。ブドウと単独REGはどちらも未入力なら総合推測から外し、入力された項目だけを使って推測します。単独REGは回数のみを入力し、分母には通常時の実践G数を使います。ブドウ逆算では差枚数を入力し、開始前G数からの実践分、実践G数の直接入力、総G数のどれを使うか選べます。推定ブドウ数と推定確率は表示だけを先に行い、「ブドウ欄へ反映」を押したときだけ通常時小役のブドウ入力へ反映します。ブドウ確率は設定1から順に 1/5.90、1/5.88、1/5.82、1/5.81、1/5.79、1/5.69、チェリー狙い時の機械割は 97.70%、98.60%、100.90%、104.00%、106.90%、111.30% です。

ネオアイムジャグラーEX では、ボーナス、通常時小役のブドウ、単独REGについて、各項目ごとの推測値を表示します。ユニメモ切替や攻略率はありません。期待値計算は公表値、チェリー狙い、フル攻略の3択で機械割を切り替えます。初期状態はチェリー狙いです。ボーナス合算は表示だけで、総合推測には含めていません。ブドウと単独REGはどちらも未入力なら総合推測から外し、入力された項目だけを使って推測します。単独REGは回数のみを入力し、分母には実践ゲーム数を使います。ブドウ逆算では差枚数を入力し、開始前G数からの実践分、実践G数の直接入力、総G数のどれを使うか選べます。G数補正は、1枚がけを3回で1Gとして扱いつつ、ボーナスごとに3回未満の端数をリセットする前提で減算します。チェリー確率は、非重複チェリー、チェリー+BB、チェリー+RBを合算した設定別確率を使います。推定ブドウ数と推定確率は表示だけを先に行い、「ブドウ欄へ反映」を押したときだけ通常時小役のブドウ入力へ反映します。ブドウ確率は設定1から順に 1/6.06、1/6.04、1/6.04、1/6.04、1/6.02、1/5.80、初期選択で使う機械割は 97.57%、98.86%、100.51%、102.27%、104.83%、107.03% です。

各項目ごとの推測値は、下のスペック表と同じまとまりごとの表で整理しています。各項目の表では、1列目の見出しにグループ名を入れ、その下に分母となるゲーム数を表示しています。各列見出しは「項目名」と「実測の数（確率）」の2行で小さく表示しています。

設定表は、機種ごとのまとまりに合わせて分けています。ハナビBH の通常時小役には2役合算を表示し、スマスロ Lハナビ と 新ハナビ の通常時小役には風鈴合算を表示します。スマスロ Lハナビ ではREGピース画面の2回以下、3～4回、5回以上の発生率も別表で表示します。スマスロ サンダーV の通常時小役にはベル合算とスイカ合算を表示します。バーサス リヴァイズ の通常時小役にはベル合算を表示し、RB終了時の赤帯は通常時、RT2連まで、RT3連以上を分けて別表で表示します。ハナハナホウオウ ～天翔～ では BIG中小役を通常時ベルの次に表示し、そのあとに BIG後トップランプ、REG中サイドランプ、連チャンサウンド選択率を並べます。ニューキングハナハナV では BIG前半パートを通常時ベルの次に表示し、そのあとに BT中ビタ押し時サイドランプ、BIG後ハイビスカスランプ、レトロサウンド発生率、REG中小役を並べ、REG後ハイビスカスランプは色と示唆内容の表を別に表示します。新ハナビ の通常時小役には氷A、チェリーA2、チェリーBも表示し、REGピース画面の0~4回時、5~6回時、7回以上時の発生率も別表で表示します。ハッピージャグラーVⅢ と Sファンキージャグラー2 と Sミスタージャグラー と ジャグラーガールズSS と ウルトラミラクルジャグラー と マイジャグラーV と ネオアイムジャグラーEX では、基本スペック表にBIG、REG、ボーナス合算、チェリー狙いの機械割を表示し、右端見出しは上段に機械割、下段にチェリー狙いを表示します。別表では公表値、チェリー狙い、フル攻略の機械割を並べます。Sファンキージャグラー2 と マイジャグラーV と ネオアイムジャグラーEX ではさらに、通常時小役と単独REGの表を別に表示します。基本スペックには通常の機械割や打ち方ごとの機械割を表示し、確率の分母は小数もそのまま見えるようにしています。

全体の見た目は、暖色系の背景とやわらかい枠線を使った従来寄りの雰囲気を保ちつつ、スマホで見やすいように横幅と余白を詰めています。スマホでも、2項目と3項目の行は横並びのまま、ラベルを入力欄の左隣に置く形にしています。さらに、スマホ時は外側の枠からはみ出しにくいように余白、文字サイズ、表の見出しの折り返し、ボタンの大きさを微調整していますが、文字や入力欄の基本配置自体は変えていません。
